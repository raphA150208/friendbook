<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @post, url: posts_path, local: true) do |form| %>
  <%= form.hidden_field :picture %>
  <%= form.hidden_field :content %>
  <p>タイトル:<%= @post.picture %></p>
  <p>本文:<%= @post.content %></p>
  <%= form.submit  "投稿する" %>
  <%= form.submit  "戻る", name: 'back' %>
<% end %>